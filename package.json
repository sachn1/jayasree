{
  "name": "jayasree-repo",
  "private": true,
  "description": "Repo-level dev tooling (linting) for jayasree. The publishable package lives in js/.",
  "type": "module",
  "scripts": {
    "lint": "eslint js/src tools/*.js demo/*.js tests/*.js",
    "test": "vitest run --coverage"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "@vitest/coverage-v8": "^4.1.10",
    "eslint": "^10.7.0",
    "vitest": "^4.1.10"
  }
}
